import { getConversations, getMessages } from '../app/actions/messages'
import messages from '../app/reducers/messages'

describe("Messages", () => {
  // it("returns a list of conversations", (done) => {
  //   getConversations("88FoheChG8").payload.promise.then(res => {
  //     console.log(messages(res.results, "88FoheChG8"))
  //     done()
  //   }, error => {
  //     console.log(error)
  //   })
  // })

  // it("returns a list of conversations", (done) => {
  //   getConversations("88FoheChG8").payload.promise.then(res => {
  //     console.log(res)
  //   }, error => {
  //     console.log(error)
  //   })
  // })
})
